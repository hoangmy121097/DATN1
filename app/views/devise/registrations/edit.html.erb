

<!--<h2>Edit <%= resource_name.to_s.humanize %></h2>
<div class="limiter">
  <div class="container-login100">
    <div class="wrap-login100">
-->
<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
<%= render "devise/shared/error_messages", resource: resource %>
  <div class="limiter">
    <div class="container-login100">
      <div class="wrap-login100">
        <form class="login100-form validate-form">
          <span class="login100-form-title"><b>Sửa Thông Tin Người Dùng</b></span>
          
          <div class="wrap-input100 validate-input">
            <span class="focus-input100" ></span>
              <%= f.label :email %><br />
              <div class="input_login"><%= f.email_field :email, autofocus: true, autocomplete: "email" %>
              </div>
            <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
              <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %>
              </div>
            <% end %>
          </div>

          <div class="wrap-input100 validate-input">
            <span class="focus-input100"></span>
            <%= f.label :Mật_khẩu %> <i>(để trống nếu bạn không muốn thay đổi nó)</i><br />
            <div class="input_login"><%= f.password_field :password, autocomplete: "new-password" %>
            </div>
          </div>
    

          <div class="wrap-input100 validate-input">
            <span class="focus-input100"></span>
            <%= f.label :Xác_nhận_mật_khẩu %>
            <i>
              <% if @minimum_password_length %>
                <em><%= @minimum_password_length %> Tối thiểu ký tự</em>
              <% end %>
            </i>
            <div class="input_login"><%= f.password_field :password_confirmation, autocomplete: "new-password" %>
            </div>
          </div>
          

          <div class="wrap-input100 validate-input">
            <span class="focus-input100"></span>
            <%= f.label :mật_khẩu_hiện_tại%> <i>(chúng tôi cần mật khẩu hiện tại của bạn để xác nhận thay đổi của bạn)</i><br />
            <div class="input_login"><%= f.password_field :current_password, autocomplete: "current-password" %>
            </div><br/>
          </div>

          <div class="container-login100-form-btn">
            <%= f.submit "Cập Nhật" , class:"login100-form-btn"%>
          </div>

          <div class="container-login100-form-btn">
            <%= link_to "Trở Lại", :back%>
          </div>
        
        </form>
      </div>
    </div>
  </div>
<% end %>



<!--<
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
 <%= render "devise/shared/error_messages", resource: resource %>
  <div class="limiter">
    <div class="container-login100">
      <div class="wrap-login100">
        <form class="login100-form validate-form">
          <span class="login100-form-title">
            <b>Sửa Thông Tin Người Dùng</b>
          </span>
        

          <div class="wrap-input100 validate-input">
            <span class="focus-input100" ></span>
              <%= f.label :email %><br />
              <div class="input_login"><%= f.email_field :email, autofocus: true, autocomplete: "email" %>
          </div>
            <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
              <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
            <% end %>


          <div class="wrap-input100 validate-input">
            <span class="focus-input100"></span>
            <%= f.label :password %> <i>(để trống nếu bạn không muốn thay đổi nó)</i><br />
            <div class="input_login"><%= f.password_field :password, autocomplete: "new-password" %>
          </div>



          <div class="wrap-input100 validate-input">
            <span class="focus-input100"></span>
            <%= f.label :password_confirmation %>
            <i>
              <% if @minimum_password_length %>
                <em><%= @minimum_password_length %> Tối thiểu ký tự</em>
              <% end %>
            </i>
            <div class="input_login"><%= f.password_field :password_confirmation, autocomplete: "new-password" %>
          
          </div>

          <div class="wrap-input100 validate-input">
            <span class="focus-input100"></span>
            <%= f.label :current_password %> <i>(chúng tôi cần mật khẩu hiện tại của bạn để xác nhận thay đổi của bạn)</i><br />
            <div class="input_login"><%= f.password_field :current_password, autocomplete: "current-password" %>
          </div><br/>
        
          <div class="container-login100-form-btn">
            <%= f.submit "Cập Nhật" , class:"login100-form-btn"%>
          </div>
          <div class="actions">
            <%= f.submit "Update" %>
          </div>
        
        </form>
    

      <div class="container-login100-form-btn">
        <%= link_to "Back", :back%>
      </div>
  </div>
    </div>
  </div>
<% end %>
-->


  

       



       
  
      
       
			
      
      

     
      